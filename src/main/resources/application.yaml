spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
server:
  address: 0.0.0.0
  port: ${PORT:8080}
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
